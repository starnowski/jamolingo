<settings>
    <servers>
        <server>
            <id>maven-central-starnowski-jamolingo</id>
            <username>${env.MAVEN_USERNAME}</username>
            <password>${env.MAVEN_PASSWORD}</password>
        </server>
    </servers>
</settings>